import styled from '@emotion/styled';

export const ScreenWrapper = styled.div`
  display: flex;
  justify-content: flex-end;
  font-size: 101px;
  margin: 60px 0 22px;
  height: auto;
  align-items: end;
  overflow: hidden;
`;
